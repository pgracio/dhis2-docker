web:
  image: pgracio/dhis2-web
  environment:
      JAVA_OPTS:
        - "-Xmx1024m -Xms4000m -XX:MaxPermSize=500m -XX:PermSize=300m -XX:-UseGCOverheadLimit"
  ports:
    - "8080:8080"
  links:
    - db
db:
  image: pgracio/dhis2-db
